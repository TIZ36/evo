# Design Context

evo 的前端只有一处：DSH（DeepSeek Harness）web 客户端插件。本文件描述它的设计上下文。

## Users

**唯一的主用户是项目作者本人，天天用。** 熟手工具：不需要教学式引导，可以有隐含约定，密度可以高。开源使用者是次要受众——首次安装不能一头雾水，但不为他们牺牲熟手效率。

用户来到这个界面的时机有两种，心态完全不同：

- **刚跑完一轮对话**，想确认 evo 到底从这轮里蒸馏出了什么。这是高频、被动、几秒钟的一瞥。
- **怀疑 agent 行为跑偏**，回来查历史上记过什么、哪条在污染上下文。这是低频、主动、要检索。

## Brand Personality

三个词：**沉静、可核查、有记账感**。

核心价值是**信任感**——用户要能确信"evo 在工作、evo 记住了这个、evo 没记错"——而不是检索效率。检索是手段，信任是目的。

翻这个界面的感觉应该像**翻实验笔记 / 研究日志**：有时序、有出处、每条都能追到它是哪次对话的产物。不是仪表盘，不是管理后台。

## Aesthetic Direction

**寄生式设计**：插件长在 DSH 里，不是贴在 DSH 上。

- **全部继承宿主 token**：`--dsw-alias-*` 语义色、`--dsw-font-*` 字阶、`--ds-ease-in-out`（DSH 唯一缓动）、`--dsw-shadow-lv1..3`。**DSH 没有圆角 token**，圆角只能硬编码 px，保持与宿主观感一致的小圆角（6-7px）。
- **不引入自己的字体、不引入自己的色板**。笔记人格靠**排版结构**建立——行长、行高、时间轴缩进、元数据字号与 tabular-nums——不靠换字体。
- **唯一的品牌资产是胶囊 mark**：容器 + 内部填充度。idle 是填满（都存好了），busy 时填充回抽再充满，2.4s 一周期。珊瑚色 `--evo-accent` (`oklch(0.68 0.19 21)`) 是插件里**唯一的非宿主颜色**，且只在 busy 时出现。
- **accent 用量压到 5% 以下**。它稀有所以有意义。
- 主题跟随宿主，明暗都要成立。

## Design Principles

1. **看得见 > 找得到。** 每一个设计决策先问"这让用户更清楚 evo 记住了什么吗"。列表只给标题是失败的——正文要能被扫到。
2. **每条记忆都要能溯源。** 从哪次会话哪一轮蒸馏出来、被召回过几次——这是信任的证据，不是可折叠的次要元数据。
3. **一个浏览处，不做浮层。** 记忆只在 Settings→Memory 浏览。composer chip 是状态指示 + 入口，不展开第二套列表。曾经的右上角浮框已按决定删除，不要复活。
4. **idle 不占地方。** evo 空闲时在界面上应该几乎不存在：chip 无边框透明，dock 行不挂载。只有它在工作或有事要说时才占据视觉预算。
5. **失败要出声。** 服务不可达时必须显性报错。静默地看起来一切正常，是唯一会彻底摧毁这个插件价值的行为。

## Hard Constraints

- 宿主不给客户端插件任何导航 API。打开 Settings→Memory 只能 DOM 驱动（按 ARIA role + 可见文本匹配，绝不依赖哈希类名）。
- 插件是 plain-JS ModuleLoader bundle，无构建期 JSX/CSS 工具链，样式靠注入 `<style>` 字符串。
- 后端 `/evo/*` 目前**只读**（status / memories / memories/:id / scopes / events）加两个动作（consolidate / import-workspace）。**没有 DELETE / PATCH / POST memory。**
- 数据规模按**上千条且持续增长**设计。
